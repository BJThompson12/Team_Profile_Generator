const inquirer = require('inquirer')
const fs = require('fs')

const generateHtml = (generatedTeamArray) => {
console.log(`from createHtml.js ${generatedTeamArray}`);
 }

module.exports = generateHtml;